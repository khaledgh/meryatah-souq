VITE_API_BASE_URL=http://localhost:8080/api/v1
# Media base URL, only used when the backend returns a RELATIVE image path.
# Leave empty when the backend already returns absolute image URLs.
# e.g. https://souq-api.linksbridge.top/media
VITE_MEDIA_BASE_URL=
